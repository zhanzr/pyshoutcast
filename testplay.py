#!/bin/python

import os
import math
import random
import audioop
import winsound

if __name__ == "__main__":
	print(len(sys.argv))
	for i in range(len(sys.argv)):
		print(sys.argv[i])
		

